namespace EventLogGenerator.GenerationLogic;

public class EventSprinkler
{
   // TODO: this should take the checkpoints in the process (which should have some reasonable timeframe) and sprinkle appropriate events into them
}